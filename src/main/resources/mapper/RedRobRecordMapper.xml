<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.RedRobRecordMapper">

    <select id="getSumByRedId" resultType="Integer">
        select sum(amount) from red_rob_record where red_packet=#{red_id}
    </select>

</mapper>